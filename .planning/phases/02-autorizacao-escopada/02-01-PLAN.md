---
phase: 02-autorizacao-escopada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Policies/EquipePolicy.php
  - app/Providers/AuthServiceProvider.php
  - bootstrap/providers.php
  - app/Models/User.php
autonomous: true
requirements:
  - RBAC-07
  - RBAC-08
  - RBAC-09
  - RBAC-10
  - MIG-01
  - MIG-02
  - MIG-03

must_haves:
  truths:
    - "EquipePolicy existe com before(), viewAny(), view(), update(), assignMembers() e é registrada em AuthServiceProvider::$policies"
    - "User::isCoordenadorGeral() retorna true para qualquer vínculo ativo com papel coord_geral, false caso contrário"
    - "User::isCoordenadorDe($equipe) retorna true apenas para papéis coord_equipe_h ou coord_equipe_m naquela equipe"
    - "User::isMembroDe($equipe) retorna true para qualquer vínculo ativo na equipe (inclui coordenadores)"
    - "AuthServiceProvider está em bootstrap/providers.php e é carregado no boot — Gate::getPolicyFor(Equipe::class) retorna EquipePolicy"
    - "users.role não foi alterado por nenhuma migration — coluna flat permanece intacta"
    - "OnlyManagerMiddleware não foi tocado — alias manager continua em bootstrap/app.php"
  artifacts:
    - path: "app/Policies/EquipePolicy.php"
      provides: "Autorização escopada por equipe (4 habilidades + before())"
      exports: ["before", "viewAny", "view", "update", "assignMembers"]
    - path: "app/Providers/AuthServiceProvider.php"
      provides: "Registro Equipe::class → EquipePolicy::class"
      contains: "protected $policies"
    - path: "bootstrap/providers.php"
      provides: "Boot do AuthServiceProvider"
      contains: "AuthServiceProvider::class"
    - path: "app/Models/User.php"
      provides: "Helpers de conveniência para autorização"
      exports: ["isCoordenadorGeral", "isCoordenadorDe", "isMembroDe"]
  key_links:
    - from: "app/Policies/EquipePolicy.php"
      to: "app/Models/User.php"
      via: "isCoordenadorGeral(), isCoordenadorDe(), isMembroDe()"
      pattern: "isCoordenadorGeral|isCoordenadorDe|isMembroDe"
    - from: "app/Providers/AuthServiceProvider.php"
      to: "app/Policies/EquipePolicy.php"
      via: "$policies array"
      pattern: "Equipe::class.*EquipePolicy::class"
    - from: "bootstrap/providers.php"
      to: "app/Providers/AuthServiceProvider.php"
      via: "providers array"
      pattern: "AuthServiceProvider::class"
---

<objective>
Instalar a camada de autorização escopada por equipe: criar EquipePolicy com 4 habilidades, AuthServiceProvider com registro explícito da policy, e três helpers booleanos no User que consultam a pivot equipe_usuario. Nenhuma migration é tocada. OnlyManagerMiddleware e users.role permanecem intactos — os dois sistemas são ortogonais.

Purpose: Toda a Phase 3 (CRUD) e Phase 4 (Atribuição) dependem deste contrato de autorização estando pronto e testado. Sem a policy, não é possível usar authorize() nos Volt SFCs.

Output: 2 arquivos novos (EquipePolicy.php, AuthServiceProvider.php), 2 arquivos modificados (bootstrap/providers.php, User.php). Nenhuma migration. Nenhuma dependência nova.
</objective>

<execution_context>
@/c/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/c/Users/andre/Desktop/pnsl-ntm/.planning/PROJECT.md
@/c/Users/andre/Desktop/pnsl-ntm/.planning/ROADMAP.md
@/c/Users/andre/Desktop/pnsl-ntm/.planning/STATE.md
@/c/Users/andre/Desktop/pnsl-ntm/.planning/phases/01-fundacao-dados-modelos/01-01-SUMMARY.md

<interfaces>
<!-- Contratos que o executor precisa — extraídos do codebase existente -->

De app/Enums/PapelEquipe.php:
```php
enum PapelEquipe: string
{
    case CoordGeral    = 'coord_geral';
    case CoordEquipeH  = 'coord_equipe_h';
    case CoordEquipeM  = 'coord_equipe_m';
    case MembroEquipe  = 'membro_equipe';
}
// Sempre usar ->value (string) em wherePivot/wherePivotIn — NUNCA o enum object
```

De app/Models/User.php (estado atual — métodos que existem e o que NÃO existe):
```php
// Existente (NÃO alterar):
public function isAdmin(): bool { return $this->role === self::ROLE_ADMIN; }
public function isCoordenador(): bool { return $this->role === self::ROLE_COORDENADOR; }

// Existente (NÃO alterar):
public function equipes(): BelongsToMany
{
    return $this->belongsToMany(Equipe::class, 'equipe_usuario', 'user_id', 'idt_equipe')
        ->using(EquipeUsuario::class)
        ->withPivot(['papel', 'usr_inclusao', 'usr_alteracao', 'dat_inclusao', 'dat_alteracao'])
        ->whereNull('equipe_usuario.deleted_at');  // soft-deletes já excluídos aqui
}

// NÃO EXISTE AINDA — criar neste plano (inserir após isCoordenador(), antes de pessoa()):
// isCoordenadorGeral(): bool
// isCoordenadorDe(Equipe $equipe): bool
// isMembroDe(Equipe $equipe): bool

// Imports a adicionar ao bloco use do User.php:
// use App\Enums\PapelEquipe;   (não está no arquivo atual)
// use App\Models\Equipe;       (não está no arquivo atual — mesmo namespace, mas import explícito exigido)
```

De app/Models/Equipe.php (padrão de wherePivot a replicar):
```php
protected $primaryKey = 'idt_equipe';  // PK do model — usar em wherePivot('idt_equipe', $equipe->idt_equipe)

// Padrão estabelecido em Phase 1 para wherePivotIn com enum:
public function coordenadores(): BelongsToMany
{
    return $this->usuarios()->wherePivotIn('papel', [
        PapelEquipe::CoordGeral->value,    // .value obrigatório
        PapelEquipe::CoordEquipeH->value,
        PapelEquipe::CoordEquipeM->value,
    ]);
}
```

De bootstrap/providers.php (arquivo atual completo — edição aditiva):
```php
<?php

use App\Providers\AppServiceProvider;
use App\Providers\VoltServiceProvider;

return [
    AppServiceProvider::class,
    VoltServiceProvider::class,
    // AuthServiceProvider::class  ← ADICIONAR aqui
];
```

De app/Providers/AppServiceProvider.php (padrão de provider a seguir):
```php
<?php

namespace App\Providers;
// imports...
use Illuminate\Support\ServiceProvider;

class AppServiceProvider extends ServiceProvider
{
    public function register(): void { }
    public function boot(): void { Gamificacao::observe(GamificacaoObserver::class); }
}
// AuthServiceProvider: trocar ServiceProvider por
// Illuminate\Foundation\Support\Providers\AuthServiceProvider as ServiceProvider
// e adicionar protected $policies = [...] antes de register()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar EquipePolicy com before() e 4 habilidades</name>
  <files>app/Policies/EquipePolicy.php</files>
  <behavior>
    - before() retorna true para isCoordenadorGeral(), null para todos os outros (NUNCA false)
    - viewAny() retorna true para qualquer usuário autenticado (filtragem por movimento é responsabilidade do scope)
    - view() retorna true se isMembroDe($equipe) OU isCoordenadorDe($equipe)
    - update() retorna Response::allow() se isCoordenadorDe($equipe), Response::deny('Apenas coordenadores da equipe podem editá-la.') caso contrário
    - assignMembers() retorna false — before() intercepta coord-geral antes de chegar aqui
    - Arquivo usa declare(strict_types=1) e namespace App\Policies
  </behavior>
  <read_first>
    - app/Http/Middleware/OnlyManagerMiddleware.php — padrão do projeto para verificação de auth (NÃO replicar a lógica flat de role aqui)
    - app/Enums/PapelEquipe.php — enum de papéis (já carregado no contexto acima)
    - app/Models/Equipe.php — confirmar primaryKey idt_equipe
  </read_first>
  <action>
Criar app/Policies/EquipePolicy.php com exatamente esta estrutura:

```php
<?php

declare(strict_types=1);

namespace App\Policies;

use App\Models\Equipe;
use App\Models\User;
use Illuminate\Auth\Access\Response;

class EquipePolicy
{
    /**
     * coord-geral tem acesso global: intercepta TODAS as habilidades antes do método específico.
     * Retorna null para não-coord-geral — a decisão flui para o método específico.
     * RBAC-10: coord-geral coexiste com users.role sem remover permissões legadas.
     */
    public function before(User $user, string $ability): ?bool
    {
        if ($user->isCoordenadorGeral()) {
            return true;
        }

        return null;
    }

    /**
     * Listar equipes: qualquer usuário autenticado pode ver a lista.
     * Filtragem por movimento (Equipe::paraMovimento) é responsabilidade do scope, não da policy.
     * RBAC-07.
     */
    public function viewAny(User $user): bool
    {
        return true;
    }

    /**
     * Ver equipe individual: qualquer membro ativo (inclui coordenadores de equipe) pode ver.
     * isMembroDe() cobre TODOS os papéis — não só PapelEquipe::MembroEquipe.
     * RBAC-07.
     */
    public function view(User $user, Equipe $equipe): bool
    {
        return $user->isMembroDe($equipe)
            || $user->isCoordenadorDe($equipe);
    }

    /**
     * Editar equipe: coord-geral já interceptado em before().
     * Método só é alcançado por não-coord-geral — coord-equipe-h/m da própria equipe.
     * Retorna Response para mensagem pt_BR na resposta 403. RBAC-07.
     */
    public function update(User $user, Equipe $equipe): Response
    {
        return $user->isCoordenadorDe($equipe)
            ? Response::allow()
            : Response::deny('Apenas coordenadores da equipe podem editá-la.');
    }

    /**
     * Atribuir membros: exclusivo de coord-geral (before() garante).
     * Este método só é alcançado se before() retornou null — nunca para coord-geral.
     * Mantido explícito para documentação e testes unitários. RBAC-07, ATRIB-01.
     */
    public function assignMembers(User $user, Equipe $equipe): bool
    {
        return false;
    }
}
```

Não criar nada além deste arquivo nesta task.
  </action>
  <verify>
    <automated>cd /c/Users/andre/Desktop/pnsl-ntm && grep -n "public function before\|public function viewAny\|public function view\|public function update\|public function assignMembers\|declare(strict_types" app/Policies/EquipePolicy.php</automated>
  </verify>
  <acceptance_criteria>
    - grep "declare(strict_types=1)" app/Policies/EquipePolicy.php retorna resultado
    - grep "public function before" app/Policies/EquipePolicy.php retorna resultado
    - grep "return null;" app/Policies/EquipePolicy.php retorna resultado (before() retorna null, não false)
    - grep "Response::deny" app/Policies/EquipePolicy.php retorna resultado (update usa Response, não bool)
    - grep "public function assignMembers" app/Policies/EquipePolicy.php retorna resultado
    - grep "return false;" app/Policies/EquipePolicy.php retorna resultado (assignMembers)
  </acceptance_criteria>
  <done>EquipePolicy.php existe com os 5 métodos corretos; before() retorna ?bool; update() retorna Response; assignMembers() retorna false.</done>
</task>

<task type="auto">
  <name>Task 2: Criar AuthServiceProvider e registrar em bootstrap/providers.php</name>
  <files>app/Providers/AuthServiceProvider.php, bootstrap/providers.php</files>
  <read_first>
    - bootstrap/providers.php — estado atual (já carregado no contexto acima)
    - app/Providers/AppServiceProvider.php — padrão de provider (namespace, estrutura dois métodos)
  </read_first>
  <action>
**Criar app/Providers/AuthServiceProvider.php:**

```php
<?php

declare(strict_types=1);

namespace App\Providers;

use App\Models\Equipe;
use App\Policies\EquipePolicy;
use Illuminate\Foundation\Support\Providers\AuthServiceProvider as ServiceProvider;

class AuthServiceProvider extends ServiceProvider
{
    /**
     * Mapa explícito Model → Policy.
     * O registro explícito em $policies tem precedência sobre o auto-discovery do Laravel 12
     * e blinda contra renomeação silenciosa do model. RBAC-09.
     *
     * @var array<class-string, class-string>
     */
    protected $policies = [
        Equipe::class => EquipePolicy::class,
    ];

    public function boot(): void
    {
        // registerPolicies() é chamado automaticamente pela base class em register()
        // via booting() callback — não chamar novamente aqui (redundante em Laravel 12).
        // Adicionar Gates extras aqui nas fases futuras se necessário.
    }
}
```

**Editar bootstrap/providers.php** — adicionar `use` import e entrada no array:

```php
<?php

use App\Providers\AppServiceProvider;
use App\Providers\AuthServiceProvider;
use App\Providers\VoltServiceProvider;

return [
    AppServiceProvider::class,
    VoltServiceProvider::class,
    AuthServiceProvider::class,
];
```

Regra de ouro: AppServiceProvider NÃO é alterado — GamificacaoObserver permanece exatamente como está.
  </action>
  <verify>
    <automated>cd /c/Users/andre/Desktop/pnsl-ntm && grep -n "AuthServiceProvider\|protected \$policies\|Equipe::class" app/Providers/AuthServiceProvider.php && grep "AuthServiceProvider" bootstrap/providers.php</automated>
  </verify>
  <acceptance_criteria>
    - grep "protected \$policies" app/Providers/AuthServiceProvider.php retorna resultado
    - grep "Equipe::class => EquipePolicy::class" app/Providers/AuthServiceProvider.php retorna resultado
    - grep "AuthServiceProvider::class" bootstrap/providers.php retorna resultado
    - grep -c "ServiceProvider::class" bootstrap/providers.php retorna 3 (App, Volt, Auth)
    - app/Providers/AppServiceProvider.php permanece intacto (GamificacaoObserver não removido)
  </acceptance_criteria>
  <done>AuthServiceProvider.php existe com $policies registrando Equipe→EquipePolicy; bootstrap/providers.php inclui AuthServiceProvider::class como terceiro elemento.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adicionar helpers isCoordenadorGeral/isCoordenadorDe/isMembroDe no User</name>
  <files>app/Models/User.php</files>
  <behavior>
    - isCoordenadorGeral() retorna true se $this->equipes()->wherePivot('papel', PapelEquipe::CoordGeral->value)->exists()
    - isCoordenadorGeral() retorna false para usuário sem vínculo, com vínculo soft-deleted, ou com papel diferente de coord_geral
    - isCoordenadorDe($equipe) retorna true para coord_equipe_h OU coord_equipe_m nessa equipe específica (FK: idt_equipe)
    - isCoordenadorDe($equipe) retorna false para membro_equipe na mesma equipe
    - isMembroDe($equipe) retorna true para QUALQUER vínculo ativo (inclui coord-geral, coord-equipe-h/m, membro-equipe)
    - isMembroDe($equipe) retorna false para usuário sem vínculo ou com vínculo soft-deleted
    - Todos os helpers usam ->exists() (O(1)), nunca ->get() ou ->load()
    - Vínculos soft-deleted são automaticamente excluídos pelo whereNull('equipe_usuario.deleted_at') já presente em equipes()
  </behavior>
  <read_first>
    - app/Models/User.php — estado atual completo (já carregado no contexto); identificar linha exata após isCoordenador() para inserção
  </read_first>
  <action>
Editar app/Models/User.php com as seguintes mudanças — nada mais:

1. Adicionar ao bloco de imports (após linha "use Illuminate\Support\Str;"):
```php
use App\Enums\PapelEquipe;
use App\Models\Equipe as EquipeModel;
```
Nota: `Equipe` está no mesmo namespace App\Models, mas import explícito é necessário para type hint no parâmetro. Usar alias `EquipeModel` evita conflito com a class Equipe já referenciada em equipes() como string-based. Alternativa: verificar se o arquivo já importa Equipe — se sim, não importar de novo. Se não importa (como confirmado no contexto: linhas 1-13 não têm Equipe), adicionar `use App\Models\Equipe;` sem alias, já que não há conflito.

VERIFICAÇÃO: O arquivo atual importa apenas:
- Database\Factories\UserFactory
- Illuminate\Database\Eloquent\Factories\HasFactory
- Illuminate\Database\Eloquent\Relations\BelongsToMany
- Illuminate\Foundation\Auth\User as Authenticatable
- Illuminate\Notifications\Notifiable
- Illuminate\Support\Str

Portanto adicionar:
```php
use App\Enums\PapelEquipe;
use App\Models\Equipe;
```

2. Inserir os três métodos após isCoordenador() (linha ~31) e antes de pessoa() (linha ~33):

```php
    /**
     * Verdadeiro se o usuário tem papel coord_geral em QUALQUER equipe ativa (não soft-deleted).
     * coord-geral é um papel GLOBAL: um vínculo em qualquer equipe basta.
     * A relação equipes() já exclui soft-deletes via whereNull(deleted_at).
     * RBAC-08 + ATRIB-01.
     */
    public function isCoordenadorGeral(): bool
    {
        return $this->equipes()
            ->wherePivot('papel', PapelEquipe::CoordGeral->value)
            ->exists();
    }

    /**
     * Verdadeiro se o usuário é coordenador H ou M da equipe específica.
     * NÃO cobre coord_geral — esse é tratado em before() da policy. RBAC-08.
     */
    public function isCoordenadorDe(Equipe $equipe): bool
    {
        return $this->equipes()
            ->wherePivot('idt_equipe', $equipe->idt_equipe)
            ->wherePivotIn('papel', [
                PapelEquipe::CoordEquipeH->value,
                PapelEquipe::CoordEquipeM->value,
            ])
            ->exists();
    }

    /**
     * Verdadeiro se o usuário tem qualquer vínculo ativo na equipe (inclui todos os papéis).
     * isMembroDe() ≠ "tem papel membro_equipe" — semântica AMPLA (qualquer vínculo).
     * Soft-deletes excluídos automaticamente pela relação equipes(). RBAC-08.
     */
    public function isMembroDe(Equipe $equipe): bool
    {
        return $this->equipes()
            ->wherePivot('idt_equipe', $equipe->idt_equipe)
            ->exists();
    }
```

REGRAS DE EDIÇÃO:
- boot() NÃO é alterado (cascata User↔Pessoa deve continuar intacta — MIG-05)
- equipes() NÃO é alterado
- $fillable, $hidden, casts() NÃO são alterados
- isAdmin() e isCoordenador() NÃO são alterados
- Ordem das imports: alfabética por convenção PSR-12 (App\ antes de Database\, Illuminate\)
  </action>
  <verify>
    <automated>cd /c/Users/andre/Desktop/pnsl-ntm && grep -n "isCoordenadorGeral\|isCoordenadorDe\|isMembroDe\|use App\\\\Enums\\\\PapelEquipe\|use App\\\\Models\\\\Equipe" app/Models/User.php</automated>
  </verify>
  <acceptance_criteria>
    - grep "isCoordenadorGeral" app/Models/User.php retorna resultado (método existe)
    - grep "isCoordenadorDe" app/Models/User.php retorna resultado (método existe)
    - grep "isMembroDe" app/Models/User.php retorna resultado (método existe)
    - grep "PapelEquipe::CoordGeral->value" app/Models/User.php retorna resultado (usa .value, não enum object)
    - grep "wherePivot('idt_equipe'" app/Models/User.php retorna 2 resultados (isCoordenadorDe e isMembroDe)
    - grep "->exists()" app/Models/User.php retorna 3 resultados (um por helper)
    - grep "boot()" app/Models/User.php retorna resultado (boot permanece intacto)
    - grep "saveQuietly" app/Models/User.php retorna resultado (cascata Pessoa permanece intacta)
    - ./vendor/bin/pest --filter=UserEquipes retorna verde (testes Phase 1 de equipes() não regridem)
  </acceptance_criteria>
  <done>User.php tem os 3 helpers novos com ->exists() e .value nos enums; boot() e equipes() intactos; testes Phase 1 continuam verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → EquipePolicy | Usuário autenticado tenta ação em equipe; Gate valida papel antes de qualquer operação de escrita |
| Pivot equipe_usuario → helpers | Helpers consultam pivot diretamente; soft-deletes devem ser excluídos automaticamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | before() em EquipePolicy | mitigate | before() retorna null (não false) para não-coord-geral; retornar false bloquearia todos incluindo admin — revisado em teste |
| T-02-02 | Elevation of Privilege | isCoordenadorGeral() com vínculo soft-deleted | mitigate | equipes() já tem whereNull('equipe_usuario.deleted_at') — helpers herdam o filtro; nunca usar DB::table() diretamente |
| T-02-03 | Elevation of Privilege | coord-geral acessando configuracoes.* | accept | Sistemas ortogonais por design — policy não substitui OnlyManagerMiddleware; TEST-07 detecta regressão |
| T-02-04 | Spoofing | URL guessing (acesso a equipe sem autorização) | mitigate | authorize() ou can() OBRIGATÓRIO em toda action de Volt SFC (Phase 3 responsabilidade); este plano não cria rotas |
| T-02-05 | Tampering | $policies silenciosamente sobrescrito por auto-discovery | accept | $policies tem precedência sobre auto-discovery no Laravel 12; risco residual documentado na AuthServiceProvider |
</threat_model>

<verification>
Após completar as 3 tasks, verificar:

```bash
cd /c/Users/andre/Desktop/pnsl-ntm

# 1. Arquivos criados/modificados existem
ls app/Policies/EquipePolicy.php app/Providers/AuthServiceProvider.php

# 2. Provider registrado
grep "AuthServiceProvider::class" bootstrap/providers.php

# 3. $policies mapeando corretamente
grep "Equipe::class => EquipePolicy::class" app/Providers/AuthServiceProvider.php

# 4. Helpers presentes no User
grep -c "isCoordenadorGeral\|isCoordenadorDe\|isMembroDe" app/Models/User.php

# 5. Suite Phase 1 não regrediu (49 testes devem continuar verdes)
./vendor/bin/pest --filter=Equipe

# 6. Pint sem diff nos arquivos novos
./vendor/bin/pint app/Policies/EquipePolicy.php app/Providers/AuthServiceProvider.php app/Models/User.php --test
```
</verification>

<success_criteria>
1. app/Policies/EquipePolicy.php existe com before()?bool, viewAny()bool, view()bool, update()Response, assignMembers()bool
2. app/Providers/AuthServiceProvider.php existe com $policies = [Equipe::class => EquipePolicy::class]
3. bootstrap/providers.php contém AuthServiceProvider::class como terceiro provider
4. app/Models/User.php contém isCoordenadorGeral(), isCoordenadorDe(Equipe), isMembroDe(Equipe) usando ->exists() e PapelEquipe::X->value
5. ./vendor/bin/pest --filter=Equipe retorna verde (sem regressão dos 49 testes Phase 1)
6. ./vendor/bin/pint --test não reporta diff nos 4 arquivos tocados
7. Nenhuma migration foi criada ou modificada (users.role intacto — MIG-01)
8. OnlyManagerMiddleware e bootstrap/app.php não foram tocados (MIG-02)
</success_criteria>

<output>
Após conclusão, criar .planning/phases/02-autorizacao-escopada/02-01-SUMMARY.md seguindo o template em @/c/Users/andre/.claude/get-shit-done/templates/summary.md
</output>

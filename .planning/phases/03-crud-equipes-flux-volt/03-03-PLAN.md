---
phase: 03-crud-equipes-flux-volt
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - resources/views/livewire/equipes/create.blade.php
  - resources/views/livewire/equipes/edit.blade.php
autonomous: true
requirements:
  - EQUIPE-06
  - EQUIPE-07

must_haves:
  truths:
    - "coord-geral cria equipe com nom_equipe e des_descricao; des_slug é auto-gerado pelo mutator se omitido"
    - "create redireciona para equipes.index após salvar com sucesso"
    - "edit carrega nom_equipe, des_slug, des_descricao e ind_ativa da equipe existente no mount()"
    - "edit salva alterações incluindo toggle de ind_ativa; redireciona para equipes.index"
    - "Validação de slug duplicado em create e em edit (com ignore do próprio registro) retorna erro de validação"
    - "Usuário sem coord-geral recebe 403 ao acessar create ou edit"
  artifacts:
    - path: "resources/views/livewire/equipes/create.blade.php"
      provides: "Formulário de criação de equipe"
      contains: "authorize('create', Equipe::class)"
    - path: "resources/views/livewire/equipes/edit.blade.php"
      provides: "Formulário de edição com toggle ind_ativa"
      contains: "authorize('update', $equipe)"
  key_links:
    - from: "equipes/create.blade.php salvar()"
      to: "Equipe::create()"
      via: "$this->validate() + Equipe::create(array_merge($validated, ['idt_movimento' => ...]))"
      pattern: "Equipe::create.*idt_movimento"
    - from: "equipes/edit.blade.php salvar()"
      to: "$equipe->update()"
      via: "$this->validate() com Rule::unique->where->ignore + $this->equipe->update($validated)"
      pattern: "equipe->update\("
    - from: "equipes/edit.blade.php mount()"
      to: "EquipePolicy::update"
      via: "$this->authorize('update', $equipe)"
      pattern: "authorize.*update.*equipe"
---

<objective>
Criar os dois Volt SFCs de escrita: equipes.create (formulário de criação) e equipes.edit (formulário de edição com toggle ind_ativa). Ambos são restritos a coord-geral via authorize() no mount().

Purpose: Completar o CRUD de equipes. O create usa o mutator de slug do model (des_slug auto-gerado quando vazio). O edit usa Rule::unique->ignore com a PK customizada idt_equipe para não conflitar com o próprio slug.

Output: resources/views/livewire/equipes/create.blade.php, resources/views/livewire/equipes/edit.blade.php
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/Desktop/pnsl-ntm/.planning/STATE.md
@C:/Users/andre/Desktop/pnsl-ntm/.planning/phases/03-crud-equipes-flux-volt/03-01-SUMMARY.md

<interfaces>
<!-- Contratos extraídos do codebase para o executor. -->

De app/Models/Equipe.php:
```php
protected $primaryKey = 'idt_equipe';
protected $fillable = ['idt_movimento', 'nom_equipe', 'des_slug', 'des_descricao', 'ind_ativa'];
protected $casts = ['ind_ativa' => 'boolean'];

// Mutator: quando des_slug está vazio, Str::slug(nom_equipe) preenche automaticamente
// Portanto: Equipe::create(['nom_equipe' => 'Oração', 'idt_movimento' => X])
//   → cria com des_slug = 'oracao' automaticamente
```

De app/Http/Requests/EquipeStoreRequest.php (Plan 01 — regras canônicas para create):
```php
'nom_equipe' => ['required', 'string', 'max:60'],
'des_slug'   => ['nullable', 'string', 'max:120', 'regex:/^[a-z0-9]+(?:-[a-z0-9]+)*$/',
                  Rule::unique('equipes', 'des_slug')->where('idt_movimento', ...)],
'des_descricao' => ['nullable', 'string', 'max:500'],
```

De app/Http/Requests/EquipeUpdateRequest.php (Plan 01 — regras canônicas para edit):
```php
// Mesmas do Store + 'ind_ativa' => ['boolean']
// Rule::unique com ->where('idt_movimento', $this->equipe->idt_movimento)
//                  ->ignore($this->equipe->idt_equipe, 'idt_equipe')
```

De resources/views/livewire/settings/profile.blade.php (analog exato):
```php
// validate inline: $validated = $this->validate([...]);
// redirect: $this->redirect(route('settings.profile'), navigate: true);
// Rule::unique já usado: Rule::unique(User::class)->ignore($user->id) — aqui adaptar para idt_equipe
```

Decisões críticas:
- D-05: Enum values snake_case — PapelEquipe::CoordGeral, CoordEquipeH, CoordEquipeM, MembroEquipe
- Resolved Q1: des_slug não exposto no formulário (auto-gerado). Mas aceito se enviado (validação presente).
- Pint remove import 'use App\Models\Equipe' em arquivos no namespace App\Models (D-13) — em Volt SFCs fora desse namespace, SEMPRE declarar o use
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar Volt SFC equipes.create com formulário de criação e validação inline</name>
  <files>resources/views/livewire/equipes/create.blade.php</files>
  <read_first>
    - resources/views/livewire/settings/profile.blade.php (linhas 1-75 — padrão exato de Volt SFC: imports, new class, mount, validate inline, redirect)
    - app/Models/Equipe.php (fillable, mutator setNomEquipeAttribute)
    - app/Http/Requests/EquipeStoreRequest.php (regras canônicas — espelhar no validate inline)
  </read_first>
  <action>
Criar resources/views/livewire/equipes/create.blade.php com a seguinte estrutura:

**PHP section:**
```php
<?php

use App\Models\Equipe;
use Illuminate\Support\Facades\Auth;
use Illuminate\Validation\Rule;
use Livewire\Volt\Component;

new class extends Component {
    public string $nom_equipe = '';
    public string $des_slug = '';
    public string $des_descricao = '';

    public function mount(): void
    {
        $this->authorize('create', Equipe::class);
    }

    public function salvar(): void
    {
        $validated = $this->validate([
            'nom_equipe' => ['required', 'string', 'max:60'],
            'des_slug' => [
                'nullable',
                'string',
                'max:120',
                'regex:/^[a-z0-9]+(?:-[a-z0-9]+)*$/',
                Rule::unique('equipes', 'des_slug')
                    ->where('idt_movimento', Auth::user()->idt_movimento),
            ],
            'des_descricao' => ['nullable', 'string', 'max:500'],
        ]);

        Equipe::create(array_merge($validated, [
            'idt_movimento' => Auth::user()->idt_movimento,
        ]));

        $this->redirect(route('equipes.index'), navigate: true);
    }
}; ?>
```

**Blade/Flux template section:**
```blade
<div>
    <div class="mb-6">
        <flux:heading size="xl">{{ __('Nova Equipe') }}</flux:heading>
    </div>

    <form wire:submit="salvar" class="my-6 w-full space-y-6">
        <flux:input
            wire:model="nom_equipe"
            :label="__('Nome')"
            type="text"
            required
            :placeholder="__('Ex.: Oração, Recepção...')"
        />
        <flux:error name="nom_equipe" />

        <flux:textarea
            wire:model="des_descricao"
            :label="__('Descrição')"
            rows="3"
        />
        <flux:error name="des_descricao" />

        <div class="flex items-center gap-4">
            <flux:button variant="primary" type="submit">
                {{ __('Salvar') }}
            </flux:button>
            <flux:button href="{{ route('equipes.index') }}" wire:navigate variant="ghost">
                {{ __('Cancelar') }}
            </flux:button>
        </div>
    </form>
</div>
```

Notas de implementação:
- des_slug NÃO exposto no formulário per resolved Q1 — o mutator preenche automaticamente
- flux:error exibe erros de validação Livewire para cada campo
- Após criar, rodar pint: `./vendor/bin/pint resources/views/livewire/equipes/create.blade.php`
  </action>
  <verify>
    <automated>
grep -n "authorize.*create.*Equipe\|Equipe::create.*idt_movimento\|Rule::unique.*idt_movimento\|redirect.*equipes.index" C:/Users/andre/Desktop/pnsl-ntm/resources/views/livewire/equipes/create.blade.php
    </automated>
  </verify>
  <done>create.blade.php existe com authorize('create'), $this->validate() espelhando EquipeStoreRequest, Equipe::create() com idt_movimento, redirect para equipes.index. Pint passa.</done>
</task>

<task type="auto">
  <name>Task 2: Criar Volt SFC equipes.edit com edição, toggle ind_ativa e Rule::unique ignore</name>
  <files>resources/views/livewire/equipes/edit.blade.php</files>
  <read_first>
    - resources/views/livewire/equipes/create.blade.php (recém criado — base para edit)
    - app/Http/Requests/EquipeUpdateRequest.php (regras com ignore — espelhar no validate inline)
    - app/Models/Equipe.php (PK idt_equipe, campo ind_ativa boolean)
  </read_first>
  <action>
Criar resources/views/livewire/equipes/edit.blade.php:

**PHP section:**
```php
<?php

use App\Models\Equipe;
use Illuminate\Validation\Rule;
use Livewire\Volt\Component;

new class extends Component {
    public Equipe $equipe;

    public string $nom_equipe = '';
    public string $des_slug = '';
    public string $des_descricao = '';
    public bool $ind_ativa = true;

    public function mount(Equipe $equipe): void
    {
        $this->authorize('update', $equipe);
        $this->equipe = $equipe;
        $this->nom_equipe = $equipe->nom_equipe;
        $this->des_slug = $equipe->des_slug ?? '';
        $this->des_descricao = $equipe->des_descricao ?? '';
        $this->ind_ativa = $equipe->ind_ativa;
    }

    public function salvar(): void
    {
        $validated = $this->validate([
            'nom_equipe' => ['required', 'string', 'max:60'],
            'des_slug' => [
                'nullable',
                'string',
                'max:120',
                'regex:/^[a-z0-9]+(?:-[a-z0-9]+)*$/',
                // ignore() com segundo argumento obrigatório (PK customizada, não 'id')
                Rule::unique('equipes', 'des_slug')
                    ->where('idt_movimento', $this->equipe->idt_movimento)
                    ->ignore($this->equipe->idt_equipe, 'idt_equipe'),
            ],
            'des_descricao' => ['nullable', 'string', 'max:500'],
            'ind_ativa' => ['boolean'],
        ]);

        $this->equipe->update($validated);

        $this->redirect(route('equipes.index'), navigate: true);
    }
}; ?>
```

**Blade/Flux template section:**
```blade
<div>
    <div class="mb-6">
        <flux:heading size="xl">{{ __('Editar Equipe') }}</flux:heading>
    </div>

    <form wire:submit="salvar" class="my-6 w-full space-y-6">
        <flux:input
            wire:model="nom_equipe"
            :label="__('Nome')"
            type="text"
            required
        />
        <flux:error name="nom_equipe" />

        <flux:input
            wire:model="des_slug"
            :label="__('Slug')"
            type="text"
        />
        <flux:error name="des_slug" />

        <flux:textarea
            wire:model="des_descricao"
            :label="__('Descrição')"
            rows="3"
        />
        <flux:error name="des_descricao" />

        <flux:switch
            wire:model.live="ind_ativa"
            :label="__('Equipe ativa')"
        />

        <div class="flex items-center gap-4">
            <flux:button variant="primary" type="submit">
                {{ __('Salvar') }}
            </flux:button>
            <flux:button href="{{ route('equipes.index') }}" wire:navigate variant="ghost">
                {{ __('Cancelar') }}
            </flux:button>
        </div>
    </form>
</div>
```

Notas de implementação:
- No edit, des_slug É exposto para edição manual (pode ser corrigido pelo coord-geral)
- flux:switch usa wire:model.live para atualizar $ind_ativa no servidor em tempo real; o salvar() persiste o valor junto com os outros campos
- Após criar, rodar pint: `./vendor/bin/pint resources/views/livewire/equipes/edit.blade.php`
  </action>
  <verify>
    <automated>
grep -n "authorize.*update.*equipe\|ignore.*idt_equipe.*idt_equipe\|equipe->update\|ind_ativa.*boolean\|redirect.*equipes.index" C:/Users/andre/Desktop/pnsl-ntm/resources/views/livewire/equipes/edit.blade.php
    </automated>
  </verify>
  <done>edit.blade.php existe com authorize('update', $equipe) no mount, Rule::unique com ->where(idt_movimento)->ignore(idt_equipe, 'idt_equipe'), $equipe->update($validated), ind_ativa boolean. Pint passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wire model → $this->validate() | Dados de formulário chegam via wire:model e devem ser validados antes de chegar ao Eloquent |
| Route {equipe} → mount() | Route model binding resolve idt_equipe — binding correto crítico para que authorize() receba o model certo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Elevation of Privilege | equipes.create mount() | mitigate | $this->authorize('create', Equipe::class) — segundo camada após ->can() na rota |
| T-03-11 | Elevation of Privilege | equipes.edit mount() | mitigate | $this->authorize('update', $equipe) com o model real carregado via route binding |
| T-03-12 | Tampering | Mass assignment via Equipe::create() | mitigate | $validated contém apenas campos da lista de regras + idt_movimento explícito; $fillable no model é terceira camada |
| T-03-13 | Tampering | des_slug editável no edit | mitigate | Rule::unique com ->where(idt_movimento)->ignore(idt_equipe) impede slugs duplicados; regex /^[a-z0-9]+(?:-[a-z0-9]+)*$/ impede slugs maliciosos |
| T-03-14 | Tampering | ind_ativa boolean em salvar() | mitigate | Validation rule 'boolean' rejeita valores não-booleanos antes de chegar ao update() |
</threat_model>

<verification>
SFCs criados:
```bash
ls C:/Users/andre/Desktop/pnsl-ntm/resources/views/livewire/equipes/
```

Padrão de autorização nos dois SFCs:
```bash
grep -n "authorize" C:/Users/andre/Desktop/pnsl-ntm/resources/views/livewire/equipes/create.blade.php
grep -n "authorize\|ignore.*idt_equipe" C:/Users/andre/Desktop/pnsl-ntm/resources/views/livewire/equipes/edit.blade.php
```

Pint sem diff:
```bash
./vendor/bin/pint resources/views/livewire/equipes/create.blade.php resources/views/livewire/equipes/edit.blade.php --test
```
</verification>

<success_criteria>
- create.blade.php: authorize('create', Equipe::class), validate() inline espelhando EquipeStoreRequest, Equipe::create() com idt_movimento, redirect para equipes.index
- edit.blade.php: authorize('update', $equipe) em mount(Equipe $equipe), Rule::unique com ignore(idt_equipe, 'idt_equipe'), $equipe->update(), ind_ativa boolean com flux:switch
- Pint --test passa em ambos sem diff
</success_criteria>

<output>
Após conclusão, criar .planning/phases/03-crud-equipes-flux-volt/03-03-SUMMARY.md
</output>

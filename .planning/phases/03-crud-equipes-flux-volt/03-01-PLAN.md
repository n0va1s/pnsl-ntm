---
phase: 03-crud-equipes-flux-volt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Requests/EquipeStoreRequest.php
  - app/Http/Requests/EquipeUpdateRequest.php
autonomous: true
requirements:
  - EQUIPE-04

must_haves:
  truths:
    - "EquipeStoreRequest::rules() exige nom_equipe (required, max:60), des_slug (nullable, unique escopado por idt_movimento), des_descricao (nullable, max:500)"
    - "EquipeUpdateRequest::rules() tem os mesmos campos mais ind_ativa (boolean), com Rule::unique ignorando o idt_equipe atual usando o segundo argumento 'idt_equipe'"
    - "Ambos os FormRequests têm messages() em pt_BR"
    - "EquipeStoreRequest::authorize() delega para Gate (can 'create' Equipe::class)"
    - "EquipeUpdateRequest::authorize() delega para Gate (can 'update' $equipe via route binding)"
  artifacts:
    - path: "app/Http/Requests/EquipeStoreRequest.php"
      provides: "Validação para store de equipe"
      contains: "Rule::unique('equipes', 'des_slug')->where('idt_movimento'"
    - path: "app/Http/Requests/EquipeUpdateRequest.php"
      provides: "Validação para update de equipe com ignore"
      contains: "->ignore($equipe->idt_equipe, 'idt_equipe')"
  key_links:
    - from: "EquipeStoreRequest"
      to: "equipes table"
      via: "Rule::unique('equipes', 'des_slug')->where('idt_movimento', ...)"
      pattern: "Rule::unique.*idt_movimento"
    - from: "EquipeUpdateRequest"
      to: "equipes table"
      via: "Rule::unique ignore com PK customizada idt_equipe"
      pattern: "ignore.*idt_equipe.*idt_equipe"
---

<objective>
Criar as duas classes FormRequest que documentam e encapsulam as regras de validação do CRUD de equipes: EquipeStoreRequest para criação e EquipeUpdateRequest para edição.

Purpose: Os Volt SFCs das Plans 02 e 03 espelham estas mesmas regras inline via $this->validate(). Os FormRequests servem como fonte canônica de documentação das regras de negócio (EQUIPE-04) e são reutilizáveis em qualquer controller HTTP futuro.

Output: app/Http/Requests/EquipeStoreRequest.php, app/Http/Requests/EquipeUpdateRequest.php
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/Desktop/pnsl-ntm/.planning/PROJECT.md
@C:/Users/andre/Desktop/pnsl-ntm/.planning/ROADMAP.md
@C:/Users/andre/Desktop/pnsl-ntm/.planning/STATE.md

<interfaces>
<!-- Contratos que o executor precisa conhecer. Extraídos do codebase. -->

De app/Models/Equipe.php:
```php
protected $primaryKey = 'idt_equipe';
protected $fillable = ['idt_movimento', 'nom_equipe', 'des_slug', 'des_descricao', 'ind_ativa'];
// Constraint DB: unique(['idt_movimento', 'des_slug'], 'equipes_movimento_slug_unique')
// Mutator: setNomEquipeAttribute preenche des_slug automaticamente via Str::slug quando des_slug está vazio
```

De app/Http/Requests/PessoaRequest.php (analog — estrutura a seguir):
```php
namespace App\Http\Requests;
use Illuminate\Foundation\Http\FormRequest;
// Sem declare(strict_types=1) — projeto não usa em Requests
class PessoaRequest extends FormRequest
{
    public function authorize(): bool { ... }
    public function rules(): array { ... }
    public function messages(): array { ... }
}
```

De app/Policies/EquipePolicy.php:
```php
// Habilidades disponíveis: 'viewAny', 'view', 'update', 'create', 'assignMembers'
// before() intercepta coord-geral (PapelEquipe::CoordGeral) e retorna true diretamente
```

Decisões críticas (STATE.md):
- D-03: PK é idt_equipe (não id) — usar ->ignore($equipe->idt_equipe, 'idt_equipe')
- D-05: Enum values snake_case (coord_geral, coord_equipe_h, coord_equipe_m, membro_equipe)
- Resolved Q1: des_slug não é exposto no formulário (auto-gerado pelo mutator) — mas o FormRequest deve validar caso seja enviado via HTTP direto
- Resolved Q3: EQUIPE-08 pertence à Phase 4, junto de ATRIB-06 + TEST-04, não ao CRUD da entidade Equipe.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar EquipeStoreRequest com regras de validação escopadas por movimento</name>
  <files>app/Http/Requests/EquipeStoreRequest.php</files>
  <read_first>
    - app/Http/Requests/PessoaRequest.php (estrutura canônica de FormRequest no projeto)
    - app/Models/Equipe.php (fillable, primaryKey, constraint unique composta)
    - app/Policies/EquipePolicy.php (habilidade 'create' para authorize())
  </read_first>
  <behavior>
    - authorize() retorna $this->user()->can('create', \App\Models\Equipe::class)
    - rules()['nom_equipe'] contém ['required', 'string', 'max:60']
    - rules()['des_slug'] contém Rule::unique('equipes', 'des_slug')->where('idt_movimento', Auth::user()->idt_movimento) — constraint composta espelhando a DB
    - rules()['des_descricao'] contém ['nullable', 'string', 'max:500']
    - messages() pt_BR para nom_equipe.required, nom_equipe.max, des_slug.unique, des_descricao.max
  </behavior>
  <action>
Criar app/Http/Requests/EquipeStoreRequest.php seguindo a estrutura exata de PessoaRequest.php:

```php
<?php

namespace App\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;
use Illuminate\Support\Facades\Auth;
use Illuminate\Validation\Rule;

class EquipeStoreRequest extends FormRequest
{
    public function authorize(): bool
    {
        return $this->user()->can('create', \App\Models\Equipe::class);
    }

    public function rules(): array
    {
        return [
            'nom_equipe' => ['required', 'string', 'max:60'],
            // des_slug é auto-gerado pelo mutator Equipe::setNomEquipeAttribute quando omitido.
            // Quando enviado explicitamente, deve ser único dentro do mesmo movimento.
            // Constraint DB: unique(['idt_movimento', 'des_slug']) — espelhar na validação PHP.
            'des_slug' => [
                'nullable',
                'string',
                'max:120',
                'regex:/^[a-z0-9]+(?:-[a-z0-9]+)*$/',
                Rule::unique('equipes', 'des_slug')
                    ->where('idt_movimento', Auth::user()->idt_movimento),
            ],
            'des_descricao' => ['nullable', 'string', 'max:500'],
        ];
    }

    public function messages(): array
    {
        return [
            'nom_equipe.required' => 'O nome da equipe é obrigatório.',
            'nom_equipe.max' => 'O nome da equipe não pode ter mais de 60 caracteres.',
            'des_slug.unique' => 'Já existe uma equipe com esse slug neste movimento.',
            'des_slug.regex' => 'O slug deve conter apenas letras minúsculas, números e hífens.',
            'des_descricao.max' => 'A descrição não pode ter mais de 500 caracteres.',
        ];
    }
}
```

Após criar, rodar pint no arquivo:
```bash
./vendor/bin/pint app/Http/Requests/EquipeStoreRequest.php
```
  </action>
  <verify>
    <automated>
grep -n "Rule::unique.*idt_movimento" C:/Users/andre/Desktop/pnsl-ntm/app/Http/Requests/EquipeStoreRequest.php && grep -n "nom_equipe.*required" C:/Users/andre/Desktop/pnsl-ntm/app/Http/Requests/EquipeStoreRequest.php && grep -n "max:500" C:/Users/andre/Desktop/pnsl-ntm/app/Http/Requests/EquipeStoreRequest.php
    </automated>
  </verify>
  <done>EquipeStoreRequest.php existe com Rule::unique escopado por idt_movimento, nom_equipe required max:60, des_descricao max:500, messages() pt_BR, pint passa sem diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar EquipeUpdateRequest com Rule::unique ignore na PK customizada</name>
  <files>app/Http/Requests/EquipeUpdateRequest.php</files>
  <read_first>
    - app/Http/Requests/EquipeStoreRequest.php (recém criado — base para update)
    - app/Models/Equipe.php (primaryKey = 'idt_equipe' — obrigatório para ignore())
  </read_first>
  <behavior>
    - authorize() retorna $this->user()->can('update', $this->route('equipe')) — route model binding resolve Equipe
    - rules()['nom_equipe'] idêntico ao Store: ['required', 'string', 'max:60']
    - rules()['des_slug'] tem Rule::unique com ->where('idt_movimento', $equipe->idt_movimento) E ->ignore($equipe->idt_equipe, 'idt_equipe') — segundo argumento é OBRIGATÓRIO porque PK não é 'id'
    - rules()['des_descricao'] idêntico ao Store: ['nullable', 'string', 'max:500']
    - rules()['ind_ativa'] = ['boolean'] — campo de toggle presente apenas no update
    - messages() pt_BR herdados do Store + 'ind_ativa.boolean'
  </behavior>
  <action>
Criar app/Http/Requests/EquipeUpdateRequest.php:

```php
<?php

namespace App\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;
use Illuminate\Validation\Rule;

class EquipeUpdateRequest extends FormRequest
{
    public function authorize(): bool
    {
        return $this->user()->can('update', $this->route('equipe'));
    }

    public function rules(): array
    {
        /** @var \App\Models\Equipe $equipe */
        $equipe = $this->route('equipe');

        return [
            'nom_equipe' => ['required', 'string', 'max:60'],
            // Constraint DB: unique(['idt_movimento', 'des_slug']).
            // ignore() usa segundo argumento 'idt_equipe' — PK customizada, não 'id'.
            'des_slug' => [
                'nullable',
                'string',
                'max:120',
                'regex:/^[a-z0-9]+(?:-[a-z0-9]+)*$/',
                Rule::unique('equipes', 'des_slug')
                    ->where('idt_movimento', $equipe->idt_movimento)
                    ->ignore($equipe->idt_equipe, 'idt_equipe'),
            ],
            'des_descricao' => ['nullable', 'string', 'max:500'],
            'ind_ativa' => ['boolean'],
        ];
    }

    public function messages(): array
    {
        return [
            'nom_equipe.required' => 'O nome da equipe é obrigatório.',
            'nom_equipe.max' => 'O nome da equipe não pode ter mais de 60 caracteres.',
            'des_slug.unique' => 'Já existe uma equipe com esse slug neste movimento.',
            'des_slug.regex' => 'O slug deve conter apenas letras minúsculas, números e hífens.',
            'des_descricao.max' => 'A descrição não pode ter mais de 500 caracteres.',
            'ind_ativa.boolean' => 'O campo ativo deve ser verdadeiro ou falso.',
        ];
    }
}
```

Após criar, rodar pint:
```bash
./vendor/bin/pint app/Http/Requests/EquipeUpdateRequest.php
```
  </action>
  <verify>
    <automated>
grep -n "ignore.*idt_equipe.*idt_equipe" C:/Users/andre/Desktop/pnsl-ntm/app/Http/Requests/EquipeUpdateRequest.php && grep -n "ind_ativa.*boolean" C:/Users/andre/Desktop/pnsl-ntm/app/Http/Requests/EquipeUpdateRequest.php && grep -n "where.*idt_movimento" C:/Users/andre/Desktop/pnsl-ntm/app/Http/Requests/EquipeUpdateRequest.php
    </automated>
  </verify>
  <done>EquipeUpdateRequest.php existe com Rule::unique tendo ->where('idt_movimento', ...) e ->ignore($equipe->idt_equipe, 'idt_equipe'), ind_ativa boolean, pint passa sem diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → FormRequest | Input não-confiável do usuário entra via formulário e precisa de validação antes de chegar ao model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | EquipeStoreRequest — des_slug | mitigate | Rule::unique com ->where('idt_movimento') impede colisão cross-movimento e injeto de slugs de outros movimentos |
| T-03-02 | Elevation of Privilege | EquipeStoreRequest::authorize() | mitigate | Delega para Gate->can('create', Equipe::class) que consulta EquipePolicy::before() — apenas coord-geral autorizado |
| T-03-03 | Tampering | EquipeUpdateRequest — ignore() na PK | mitigate | Segundo argumento 'idt_equipe' obrigatório para que o ignore funcione corretamente com PK customizada; sem ele, ignore() usa 'id' e não exclui o registro correto |
| T-03-04 | Tampering | Mass assignment via FormRequest | accept | $fillable no model Equipe já protege; FormRequest é camada adicional de validação de tipos e regras de negócio |
</threat_model>

<verification>
Ambos os FormRequests criados:
```bash
grep -l "EquipeStoreRequest\|EquipeUpdateRequest" C:/Users/andre/Desktop/pnsl-ntm/app/Http/Requests/*.php
```

Pint passa sem diff:
```bash
./vendor/bin/pint app/Http/Requests/EquipeStoreRequest.php app/Http/Requests/EquipeUpdateRequest.php --test
```

Sintaxe PHP válida:
```bash
C:/xampp/php/php.exe -l C:/Users/andre/Desktop/pnsl-ntm/app/Http/Requests/EquipeStoreRequest.php
C:/xampp/php/php.exe -l C:/Users/andre/Desktop/pnsl-ntm/app/Http/Requests/EquipeUpdateRequest.php
```
</verification>

<success_criteria>
- EquipeStoreRequest.php e EquipeUpdateRequest.php existem em app/Http/Requests/
- EquipeStoreRequest tem Rule::unique('equipes','des_slug')->where('idt_movimento', Auth::user()->idt_movimento)
- EquipeUpdateRequest tem Rule::unique()->where('idt_movimento', $equipe->idt_movimento)->ignore($equipe->idt_equipe, 'idt_equipe')
- Ambos têm messages() em pt_BR
- pint --test passa sem diff em ambos os arquivos
- C:/xampp/php/php.exe -l retorna "No syntax errors" em ambos
</success_criteria>

<output>
Após conclusão, criar .planning/phases/03-crud-equipes-flux-volt/03-01-SUMMARY.md
</output>
